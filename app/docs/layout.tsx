// Applies the shared documentation styles (app/docs/docs.css) to every /docs route.
import "./docs.css";

export default function DocsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
